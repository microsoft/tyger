job:
  codespec:
    image: eminence.azurecr.io/recon:current
    buffers:
      inputs:
        - input
      outputs:
        - output
    args:
      - "-i"
      - "$(INPUT_PIPE)"
      - "-o"
      - "$(OUTPUT_PIPE)"
    resources:
      requests:
        cpu: 1
