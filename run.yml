job:
  codespec:
    image: mcr.microsoft.com/cbl-mariner/base/core:2.0
    buffers:
      inputs:
        - input

    command:
      - bash
      - -c
      - |
        # set -euo pipefail
        cat $INPUT_PIPE > /dev/null
        # sleep infinity
