job:
  codespec:
    image: mcr.microsoft.com/azurelinux/base/core:3.0
    buffers:
      inputs:
        - input
      outputs:
        - output

    command:
      - bash
      - -c
      - |
        sleep 10
        cat $INPUT_PIPE > $OUTPUT_PIPE
