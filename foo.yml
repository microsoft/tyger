job:
  codespec:
    image: ubuntu
    buffers:
      inputs:
        - input
      outputs:
        - output
    command:
     - bash
     - -c
     - |
      set -euo pipefail
      cat $INPUT_PIPE > $OUTPUT_PIPE
      # while true; do
      #   date
      #   sleep 1
      # done

  # buffers:
  #   input: _
  #   output: _
