job:
  codespec:
    image: ubuntu
    buffers:
      inputs:
        - input
      outputs:
        - output
    command:
      - bash
      - -c
      - |
        echo "Hello $(cat $INPUT_PIPE)" > $OUTPUT_PIPE
