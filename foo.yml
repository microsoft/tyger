job:
  codespec:
    image: ubuntu
    buffers:
      inputs:
        - input
      outputs:
        - output
    command:
     - bash
     - -c
     - |
      set -euo pipefail
      cat $INPUT_PIPE > $OUTPUT_PIPE

  buffers:
    input: _
    output: _
